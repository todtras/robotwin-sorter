# Python 3.11 권장
#   3.13에서는 PyBullet 휠이 없어 소스 빌드로 넘어가 실패할 수 있습니다.
#
# 설치:
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)
#   pip install -r requirements.txt

pybullet==3.2.6
opencv-python==4.10.0.84
numpy>=1.24,<2.0
matplotlib>=3.7

# 추론 전용. 학습은 Colab에서 합니다.
# 설치 용량이 2GB+ 라 사내 정책에 막힐 수 있습니다.
# 막히면 ONNX 내보내기 후 onnxruntime으로 대체하세요 (훨씬 가볍습니다).
ultralytics>=8.2
